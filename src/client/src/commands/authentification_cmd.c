/*
** EPITECH PROJECT, 2023
** commands_handler
** File description:
** commands_handler
*/

#include "client.h"

int user_login(client_t *client, char **param)
{
    printf("loginnnnn\n");
    return 0;
}

int user_logout(client_t *client, char **param)
{
    printf("logoutttttt\n");
    // reply_format(client->addrs.clients[index].fd, C214);
    // close(client->addrs.connections[index].fd);
    // client->addrs.connections[index].fd = -1;
    // client->addrs.connections[index].data_fd = -1;
    // client->addrs.connections[index].is_anonyme = false;
    // client->addrs.connections[index].is_logged = false;
    // client->addrs.connections[index].ambivalence = AMBI_NULL;
    return 0;
}
