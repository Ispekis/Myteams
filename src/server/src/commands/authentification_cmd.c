/*
** EPITECH PROJECT, 2023
** commands_handler
** File description:
** commands_handler
*/

#include "server.h"

int user_login(server_t *server, char **param, int index)
{
    return 0;
}

int user_logout(server_t *server, char **param, int index)
{
    // reply_format(server->addrs.clients[index].fd, C214);
    // close(server->addrs.connections[index].fd);
    // server->addrs.connections[index].fd = -1;
    // server->addrs.connections[index].data_fd = -1;
    // server->addrs.connections[index].is_anonyme = false;
    // server->addrs.connections[index].is_logged = false;
    // server->addrs.connections[index].ambivalence = AMBI_NULL;
    return 0;
}
